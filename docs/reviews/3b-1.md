# Stage 3b-1 검토 — 애널리스트 4분할 + Research Director

## 스코프

### 신규
- `aegisvest/agents/tools.py` — CrewAI 툴 래퍼 (`news_scraper` / `fundamentals` /
  `technical_indicators`). 순수 함수 → JSON 직렬화만.

### 변경
- `aegisvest/agents/crew.py` — **전면 재작성**. ① Macro + ②③④ 애널리스트(async) → ⑤
  Research Director → ⑧ CIO (6 태스크). `_Spec` 레지스트리 + `_DIARY` dispatch.
- `aegisvest/schemas.py` — `AnalystNote`/`AnalystView` 제거, `FundamentalNote(s)` /
  `ThematicNote(s)` / `EventRisk` / `MarketNarrative` / `SleeveStance` / `ResearchView` 추가.
  `CrewOutcome` 필드 6종으로.
- `aegisvest/agents/guardrails.py` — `no_fabricated_numbers(hedge_only=)` (툴 쓰는 ①②③).
- `aegisvest/report.py` — 조직 노트 섹션 (News + RD + CIO).
- `config/agents.yaml` / `config/tasks.yaml` — 6 에이전트/태스크.
- 정방향: `diary.logger` (claim_type `catalyst`/`event_risk`/`sleeve_stance` — phase-4 §2.1 통제어휘),
  `main.py` (crew.cio.verdict 만 소비 — 무영향).

근거: report/phase-3 §3(②③④⑤)·§4·§5, phase-4 §2.1, docs/PROMPTS.md, add-agent 스킬.

## Round 1 — 1건

- [논리] 테마 애널의 `theme_strength_adj`(-0.2~0.2 권고값)가 `no_fabricated_numbers` 에
  걸림. → 처음엔 extra_allowed 로 우회했으나 Round 2 에서 근본 수정.

## Round 2 — 1건

- [논리] ②③ 애널리스트가 이제 **툴(fundamentals/technical)** 을 갖는데,
  `no_fabricated_numbers` 는 툴 반환값을 페이로드로 못 잡아 정당한 툴 수치(ROE 31% 등)를
  거부 → 재시도 루프. → `hedge_only` 플래그: 툴 쓰는 ①②③ 은 헤지 표현("약 15%")만 거부,
  숫자-페이로드 대조는 생략. ④⑤⑧ 은 전체 검사 유지. `theme_strength_adj` 문제도 해소.

수정 후 ruff / format / mypy(43) / pytest(190, +1 deselected) 통과.

## Round 3

- 수정 필요 0건. `hedge_only` 동작 테스트 추가 (test_guardrails).

### 검토했으나 finding 아님
- `async_execution=True` (①②③④) — mock 은 동기라 병렬성 미검증. 실 DeepSeek(잔액부족)
  으로도 미검증. crewai 규약(async 먼저, 마지막은 sync)은 준수. 잔액 충전 시 `pytest -m llm`.
- `FundamentalNotes/ThematicNotes` 를 `{notes: [...]}` 로 래핑 — crewai `output_pydantic` 이
  BaseModel 필요 (bare list 불가). phase-3 §3 의 리스트 스펙과 구조만 다름.
- 툴 인스턴스를 3개 에이전트가 공유 (`_AGENT_TOOLS`) — crewai 관행상 무해 (툴 무상태).
- ⑤ RD 의 sleeve_stance 는 매주 diary(`sleeve_stance`, horizon 12주) 기록 — 의도됨
  (phase-4 §2.1 채점 대상).
- 툴은 실 LLM 에서만 호출됨 — 3b-1 mock 테스트는 툴 미경유 (ScriptedLLM `supports_function_calling=False`).

## 라이브 검증
mock(ScriptedLLM) 로 6-에이전트 크루 full path (async 태스크·context 체인·6 출력 파싱·
diary 6 claim_type·guardrail hedge_only) — 22 tests. 실 DeepSeek 은 잔액 부족.

## 최종
- ruff / ruff format / mypy(43 files) / pytest **190 passed, 1 deselected** 통과.
- 커밋: `build(3b-1): 애널리스트 4분할 (②③④) + ⑤ Research Director + 크루 툴`
