# Stage 4-0 검토 — 판단 일기 태깅 하네스 완성 (통제 어휘 + signal_rules + 원자료 스냅샷)

3a-9/3b 때 만든 `diary/schema.py::derive_tags()` 는 자리표시자였다 (통제 어휘 검증 없음,
`signal:*` 자동 평가 없음, 호출부가 `regime`/`claim_type` 만 채워 넣음). Phase 4 착수 전
report/phase-4 §7 대로 완성. evaluate.py/Reviewer/RAG 의 기반이라 먼저 마감.

## 스코프

### 신규
- `config/diary_taxonomy.yaml` — CLOSED/SEMI-OPEN 통제 어휘 + `signal_rules`(10종) +
  `max_tags_per_entry`/`similarity_floor`/`recency_halflife_months`. report/phase-4 §7.1.

### 변경
- `aegisvest/diary/schema.py` — `Taxonomy` 모델 + `diary_taxonomy()` 로더, `CLAIM_TYPES` 를
  하드코딩 frozenset 대신 taxonomy 에서 파생, `eval_signal_rules(snapshot)`(신뢰 config 만
  eval, 결측/타입불일치는 조용히 미발동), `magnitude_of(delta_pp)`, `derive_tags()` 에
  CLOSED 값 검증(미지값 드롭) + `data_snapshot` 인자로 signal 자동 평가 + 태그 8개 상한.
- `aegisvest/schemas.py` — `PipelineResult.macro: MacroData` 필드 추가 (일기 원자료 소스).
- `aegisvest/pipeline.py` — `run_pipeline` 이 `macro` 를 `PipelineResult` 에 포함.
- `aegisvest/agents/crew.py` — `_dc_*` 빌더가 `pr` 을 받아 `action`(crisis_shift/exclusion/
  catalyst_bet/hold) 산출, `_diary_snapshot` 이 `pr.macro.model_dump()` 병합, `_log_diary`
  가 `rates_dir`·`data_snapshot` 전달.
- `aegisvest/agents/organization.py` — `allocation_tilt` 에 `sleeve`(최대 틸트 카테고리)·
  `action`(defensive/offensive)·`magnitude_of()`·`rates_dir` 계산, `risk_veto` 에 `action:veto`.
- `tests/fixtures/pipeline.py` — `macro` 필드 추가 (`crisis=True` 시 vix/hy_oas 실제 상승).
- `tests/test_diary.py`, `tests/test_agents/test_organization.py` — signal_rules·태그 검증·
  통제어휘 거부 테스트 8종 추가.

## Round 1 — 0건 (신규 빌드, 사전 설계 검토로 결함 없이 통과)

라이브 스모크: crisis fixture(vix 40>vix3m 35, hy_oas_4w_change_bp 150) → org 실행 →
`regime_call` 태그에 `action:crisis_shift`, `rates_dir:hiking`,
`signal:credit_spread_widening`, `signal:vix_term_backwardation` 자동 발생, `data_snapshot`
에 `hy_oas_bp: 800.0` 등 원자료 그대로 확인.

수정 후 ruff / format / mypy(45) / pytest(197, +1 deselected) 통과. 신규 테스트 8종 포함.

### 검토했으나 finding 아님
- `eval()` 사용 — 실행 대상은 우리가 직접 관리하는 `config/diary_taxonomy.yaml` 뿐 (외부/
  LLM 입력 아님), `__builtins__` 차단 + try/except 로 결측 필드 안전 처리. 신뢰 경계 내.
- ~~`golden_cross`/`death_cross` 룰이 `MacroData` 에 없는 전일 SMA 참조 → 미발동~~
  1차 해소(감사 후속): `spx_sma_*_prev` 추가 + 1거래일 교차 판정. **재수정 (2026-09-02,
  post-e2e review)**: 주간 크루 주기와 1거래일 창이 안 맞아 상태 기반(`spx_sma_50 < spx_sma_200`)
  으로 전환, `spx_sma_*_prev` 필드 제거. reviews/post-e2e-codereview.md.
- 태그 8개 상한을 넘으면 나열 순서(핵심 6종 → signal) 로 잘림, 중요도 우선순위 없음 —
  phase-4 §7.1 에 우선순위 규정 없어 단순 절단으로 충분 (ponytail).

## 최종
- ruff / ruff format / mypy(45 files) / pytest **197 passed, 1 deselected** 통과.
- 커밋: `build(4-0): 판단일기 태깅 하네스 완성 (taxonomy + signal_rules + macro 스냅샷)`
