# Stage 3a-10 검토 — report.py + main.py (주간 크루 오케스트레이션)

## 스코프

### 신규
- `aegisvest/main.py` — `run(trigger)` / `main()`. 적금 납입 → run_pipeline → run_crew(키 있을 때)
  → CIO APPROVED 면 체결·HOLD 면 스킵 → mark-to-market → 리포트 + 알림.
- `aegisvest/report.py` — `weekly_report_md` / `mattermost_summary` / `write_run` /
  `performance_report_md` + `python -m aegisvest.report performance|last` CLI.
- `aegisvest/tools/fx.py` — `usd_krw` (KRW=X 환율).
- `tests/{test_main,test_report}.py`, `tests/test_tools/test_fx.py`, `tests/fixtures/pipeline.py`.

### 영향 (impact scope)
- `aegisvest/watchdog.py` — `_mark_nav`(일일 NAV 평가) + `_trigger_weekly_crew`(위기→main) +
  `_trading_day` 추가. `run()` 에 NAV 마킹 삽입.
- `aegisvest/tools/_prices.py` — `latest_close_date` 공용 헬퍼 (감시견·main NAV 인덱스 통일).
- `aegisvest/tools/rebalance.py` — **쿨다운을 매수 루프에서 제거** (아래 R1).
- `aegisvest/config.py` — `monthly_contribution_krw` (env `MONTHLY_CONTRIBUTION_KRW`, 기본 10만).
- `aegisvest/schemas.py` — `WeeklyRunResult`.
- 정방향 호출: run_pipeline / paper / benchmarks / metrics / shadow / crew.run_crew / state.
- `tests/conftest.py` (env delenv), `tests/test_watchdog.py` (통합 no-op 픽스처),
  `tests/test_agents/conftest.py` (fixtures/pipeline 로 리팩터).

근거: report/phase-2 §3.3·§4·§7, phase-3 §4·§7.3·§9, 사용자 결정(CIO HOLD 권한).

## Round 1 — 5건

- [명세대조] `rebalance.py` — 쿨다운(`_cooldown_hit`)이 **매수 루프에서도** 체크됐다.
  phase-2 §3.3 ⑦ "최근 10거래일 내 조정한 카테고리는 **6단계(매도)** 건너뜀" — 신규 현금
  매수는 세금 0이라 제한 없음. → 매수 루프에서 제거, 매도 루프만 유지. 3a-6 잠복 이슈,
  3a-10 에서 main 이 `cooldown_days` 를 실제 유지하며 표면화.
- [논리] `main.py` 가 `crisis_flag.json` 을 "소비 표시"로 덮어썼다 — 이 파일은 감시견이
  라이브 위기상태를 유지하는 소유물. 진행 중 위기를 main 이 꺼버림. → main 은 읽지도 쓰지도
  않음 (위기 트리거는 감시견이 `_trigger_weekly_crew` 로 직접).
- [논리] `main.py` 가 `pf.cooldown_days` 를 갱신하지 않아 쿨다운 가드레일이 무력. →
  `_bump_cooldown`: 매도한 카테고리 0 리셋, 나머지 +5(주간≈5거래일). CRISIS·HOLD 처리.
- [논리] NAV 마킹 날짜가 `pr.as_of`(= macro 데이터 max 날짜, FRED 지연으로 1~2일 뒤처짐)
  vs 감시견 `_trading_day`(실제 종가일) 불일치 → 근접 중복 NAV 포인트. → 공용
  `_prices.latest_close_date` 로 통일.
- [논리] `watchdog._mark_nav` 가 포트폴리오 없어도 `_trading_day`(네트워크) 호출 → 테스트
  행. → 포트폴리오 체크를 먼저, 거래일 조회는 그 다음.

수정 후 ruff / format / mypy(42) / pytest(189, +1 deselected) 통과.

## Round 2 — 1건

- [논리] main NAV 마킹이 여전히 `pr.as_of` 사용 (R1 은 감시견만 수정). → main 도
  `latest_close_date` 로 마킹, `_bump_cooldown` 직후.

수정 후 전체 통과.

## Round 3

- 수정 필요 0건. 검토 종료.

### 검토했으나 finding 아님 (의도된 단순화)
- 체결가 = 파이프라인 마지막 종가 (phase-3 §7.2 "다음 개장가" 아님). 라이브 페이퍼는
  일요일→금요일 종가, 실제 월요일 개장가와 소폭 차이 (슬리피지 없음). 3a-11 백테스트는
  다음 바 open 으로 루프.
- 위기 트리거 시 감시견(06:30 KST)이 main 을 동기 실행 — 미국장 폐장 중이라 마지막 종가로
  체결. 위기 대응은 정밀 가격보다 속도 우선. 드문 이벤트.
- 쿨다운 +5/주 는 근사 (crisis 중간 실행 시 과다 카운트) — CRISIS 는 쿨다운 예외라 무해.
- shadow.deterministic = pf (조직 포트는 3b) — 매주 pf 미러링, 3b 배선 준비.

## 라이브 검증
`python -m aegisvest.main` (실 FRED/yfinance):
- 크루가 DeepSeek 402 로 실패 → **try/except 로 흡수, 결정론 결과로 진행** (설계대로)
- 적금 납입 $72.59 (10만 KRW) → 10종목 매수 (각 $2.42, max_change 램프) → cash $50.81
- NAV $72.57 @ 2026-08-31 (latest_close_date) · 벤치 3종 ~$72.59 · constraints PASS
- 쿨다운 {999,999,999} (매도 0) · report.md (1.1KB) + pipeline/execution.json 저장
- Mattermost 404 (웹훅 URL 만료) → graceful degradation, 파일만

## 최종
- ruff / ruff format / mypy(42 files) / pytest **189 passed, 1 deselected** 통과.
- 커밋: `build(3a-10): report.py + main.py 주간 오케스트레이션 + 일일 NAV 평가`
