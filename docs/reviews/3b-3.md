# Stage 3b-3 검토 — 섀도 A/B 이중 포트폴리오 병행 추적

## 스코프

### 변경
- `aegisvest/main.py` — `paper_portfolio.json` 단일 포트 → `ShadowState`(deterministic +
  organization) 이중 추적. `_load_shadow`(구버전 1회 이관), `_execute_and_mark`(양쪽 체결·마크).
  조직 없으면(키 없음) 두 포트 동일 경로. 조직 있으면 `det_pr = run_pipeline(det_pf)` 2차 실행.
- `aegisvest/watchdog.py` — `_mark_nav` 이 섀도 양쪽 포트 마크 (paper_portfolio.json → shadow.json).
- `aegisvest/report.py` — `performance_report_md` 는 `shadow.organization` 기준 + 섀도 A/B
  상세(결정론 vs 조직 총수익·MDD·샤프). `weekly_report_md` NAV 라인에 결정론 병행 NAV.
- `aegisvest/broker/shadow.py` — `delta_report` verdict: `|delta| < 1e-4` → `insufficient_data`
  (동일=크루 미실행 케이스가 "harmful" 로 안 나오게).

근거: report/phase-3 §1(C)·§7.3.

## Round 1 — 1건

- [UX] `delta_report` 가 조직·결정론 NAV 동일(크루 미실행)일 때 `org_neutral_or_harmful`
  판정 → 리포트가 "조직이 해롭다"고 오해 유발. → `|delta| < 1e-4` 는 `insufficient_data`.

수정 후 ruff / format / mypy(45) / pytest(197, +1 deselected) 통과.
라이브: 프레시 상태에서 `python -m aegisvest.main` → 크루 402 흡수 → 양쪽 포트 10종목
동일, delta 0.00%, `insufficient_data`, 성과리포트 섀도 A/B 섹션 정상.

## Round 2

- 재검토 0건.

### 검토했으나 finding 아님 / 후속
- 조직 크루 실행 시 `run_pipeline` 2회 (org_pf, det_pf) — 스크린·스코어는 파일캐시라
  네트워크 0, ~수초. 리밸런싱 플랜만 보유현황 따라 다름.
- 결정론 병행 시뮬은 CIO HOLD 와 무관하게 진행 (A/B 는 "조직 개입이 없었다면" 을 측정하므로
  결정론은 항상 자기 경로를 따라야 함).
- `paper_portfolio.json` 폐기 — `_load_shadow` 가 구버전 있으면 organization 으로 이관
  (deterministic 은 deep copy 로 동일 시작).
- Gate B "동기간 MDD ≤ SPY MDD" 등 판정은 12개월 데이터 후 (phase-2 §6.5).
- 조직 vs 결정론 회전율·세금 비교 — 3~6개월 데이터 축적 후 별도 분석.

## 최종
- ruff / ruff format / mypy(45 files) / pytest **197 passed, 1 deselected** 통과.
- 커밋: `build(3b-3): 섀도 A/B 이중 포트폴리오 병행 추적`
