# Stage 3a-4 검토 (감시견)

## 스코프

**신규**
- `aegisvest/state.py` — state/ JSON persistence (load/save model·list, degrade on 손상)
- `aegisvest/notify.py` — Mattermost 인커밍 웹훅 (`post()`), 미설정 시 로그만
- `aegisvest/watchdog.py` — 일일 감시견 (`run()` / `main()`)
- `tests/test_state.py` (5) · `tests/test_watchdog.py` (9)

**변경 (영향)**
- `config/regime_rules.yaml` · `rules.py::Crisis` — `vix_1d_spike_pct: 0.50` 추가 (phase-2 §3.2 4번째 위기 조건).
- `aegisvest/tools/regime.py::_crisis` — VIX 1일 변화율 > +50% → CRISIS 사유 추가. **regime_score 가 위기 판정의 단일 권위** (감시견은 `result.crisis_active` 만 소비).
- `aegisvest/schemas.py` — `CrisisFlag` 추가.

**영향받는 이전 단계**
- `tools/regime.py::regime_score` — 감시견이 첫 실사용 소비자. 3-인자 시그니처(macro/history/crisis_state) 정상.
- `config.py::ensure_runtime_dirs` / `tools/macro_data.py` — 소비, 미변경.
- `test_regime.py` — `vix_1d_change_pct` 기본 0.0 (macro fixture) → 신규 위기 조건이 기존 테스트에 오탐 안 함. 확인.

## Round 1

- **[테스트 커버리지]** `_crisis` 의 신규 `vix_1d_change_pct > 0.50` 분기에 직접 테스트 없음. → `test_crisis_vix_1d_spike_override` 추가 (경계 0.50 포함).
- **[테스트 커버리지]** `watchdog` 같은 날 재실행이 값을 *덮어쓰는지* 미검증 (건수만). → `test_same_day_updates_not_appends` 에 `total_score` 갱신 검증 추가.

## Round 2

- 변경분(테스트 2건 추가)만 — 프로덕션 코드 무변경. `_crisis` VIX 스파이크 로직 재검토: 단위(소수 0.60 vs 0.50), 부호(상승만), 포맷(`:+.0%`) 정상. **수정 필요 0건. 검토 종료.**

## 설계 노트 (수정 안 함)

- 감시견은 크루를 **직접 실행하지 않는다** — `state/crisis_flag.json` 을 쓰고 `main.py`(3a-10)가 실행 시 읽어 즉시 크루를 돌린다. subprocess 스폰 회피.
- `crisis_state.json`(regime 래치 + 5거래일 타이머) 과 `crisis_flag.json`(운영/main.py 신호) 은 별도 파일이나 감시견이 같은 `result` 로 둘 다 갱신 → 동기 유지. flag 수동 삭제 시 다음 실행이 self-heal.
- `_trading_days_between` 는 `np.busday_count` — 미국 공휴일 무시(근사). phase-2 명세엔 정밀 캘린더 요구 없음. 백테스트에서 필요 시 `numpy.busdaycalendar` 로 교체.
- 히스토리 캡 `_MAX_HISTORY=40` (EMA span 5 에 충분).

## 최종

`ruff check .` / `ruff format --check` / `mypy aegisvest` / `pytest -q` (72 passed).
라이브: `python -m aegisvest.watchdog` 2회 실행 정상 — FRED 키 없어 2축 → low_confidence → history 미기록, `crisis_state.json` persist, 알림 없음.
