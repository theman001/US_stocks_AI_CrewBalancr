# Stage 4-4 검토 — DiaryRAG.recall() + O-A 랭킹 + P-D 주입 + 에이전트 연결

## 스코프

### 신규
- `aegisvest/diary/rag.py` — `DiaryRAG.recall(query_text, situation_tags, k=4)` (Q-D top-40×2
  → O-A 랭킹 `0.65·코사인+0.20·구조매칭+0.15·최근성` → dedupe → floor·attribution 게이트
  → crisis 다양성), `build_query` (§6.2 결정론 태깅, LLM 불필요), `format_recall` (§6.4 P-D —
  top-1 중간요약 + 카드), `RecalledCase` NamedTuple, 헬퍼 `_hits`/`_structural`/`_recency`/
  `_months_since`/`_diversify`/`_short_tags`.
- `aegisvest/agents/organization.py::_recall_block` — 회상 1회 계산·포맷 (콜드스타트·오류 → "").
- `aegisvest/agents/crew.py` — `_RECALL_TASKS` (①②③⑤⑥⑦), `_numbers_in` (extra_desc 수치 →
  guardrail `extra_allowed`), `make_task` 가 회상/반려 텍스트 수치를 허용.
- `aegisvest/diary/schema.py` — `approx_tokens`/`clip_tokens` (reviewer·rag 공용, §4.2/§6.4).
- `tests/test_diary_rag.py` +7, `tests/test_agents/test_organization.py` +1 (주입 위치 검증).

### 변경
- `crew.run_analysts` — `diary_recall: str = ""` 파라미터, ①②③⑤ 태스크 description 에 append.
- `organization._Ctx` — `recall` 필드, `_Ctx.run` 이 ⑥⑦ 에 prepend (⑧ CIO 제외).
- `organization.run_organization` — `_recall_block(pr)` 계산 후 `run_analysts` + `_Ctx` 에 전달.
- `diary/reviewer._cap_lesson_card` — 자체 근사 → `schema.clip_tokens` 위임 (중복 제거).

### 영향 (검토 포함)
- `no_fabricated_numbers(extra_allowed=)` — 기존 파라미터, 회상 카드 수치(결정론 생성분)를
  guardrail 이 fabrication 으로 오탐하지 않도록. ⑤⑦(full check) 에서 특히 필요.
- 6개 판단 태스크 description 이 회상 블록(~350-450 토큰)만큼 길어짐. `{...}` 플레이스홀더
  미사용 (extra_desc append) → tasks.yaml 무변경.
- `PipelineResult` — `_diary_snapshot(pr)` 로 읽기 (organization 기존 헬퍼 재사용).

## Round 1 — 4건

- [명세대조] `recall` 이 필터를 dedupe **전에** 수행 → 한 항목의 약한 벡터(situation) 하나로
  더 강한 벡터(lesson)까지 탈락. §6.3 은 dedupe(max rank) → 필터 순서. → 재구조화, `RecalledCase`
  에 `attribution` 추가.
- [논리] `build_query` 가 쿼리 태그에 `claim_type:regime_call` 을 고정 주입 → 현재 상황엔
  claim 이 없는데 regime_call 항목에 구조매칭 편향. → `regime:`·`signal:` 태그만.
- [논리] `_short_tags` 가 `:` 없는 태그에 IndexError (`t.split(":")[1]`). → `":" in t` 가드.
- [논리] `_hits` 가 `dists[i]` 인덱스 미가드 (chroma 응답 길이 불일치 시). → `i < len(dists)`.

수정 후 ruff / format / mypy(48) / pytest **237 passed, 2 deselected** 통과.

## Round 2 — 0건

### 검토했으나 finding 아님 / 의도된 단순화
- **단일 회상 (①②③⑤⑥⑦ 공용)** — §6.2 는 event/theme 를 애널리스트 출력에서 취득하나
  회상은 그 전(§6.1)이라 모순. signal(스냅샷 기반)만 쿼리에 포함, 2단계 회상(①②③ 매크로만
  → ⑤⑥⑦ 강화)은 4-6. `report/phase-4 §6` 결정 노트.
- **`format_recall` top-1 중간요약이 스펙 예시(상황/판단/결과/교훈 4줄)보다 단순** — 헤더
  `[월 · verdict ±score · 유사도]` + 원문 120토큰 절삭. situation_text 가 이미 `[상황]…` 형식.
  풍부한 템플릿화는 4-6 폴리시.
- **회상을 크루 실행 중 1회 임베딩** (bge-m3 로드 ~1-3s) — §5.2 예상 범위, 콜드스타트엔
  `count()==0` 조기 반환으로 임베딩 자체를 스킵.
- **guardrail `extra_allowed` 에 회상 수치 통과** — 카드 수치는 채점된 실제 결과 + 파이썬
  생성 lesson_card 라 fabrication 아님. hedge 표현은 여전히 거부.
- `_diversify` 가 k 미만 반환 가능 (후보 부족) — 스펙 "top k" 와 부합, 콜드~얇은 일기 정상.

## 최종
- ruff / ruff format / mypy(48 files) / pytest **237 passed, 2 deselected** 통과.
- 커밋: `build(4-4): DiaryRAG.recall + O-A 랭킹 + P-D 주입 + 에이전트 연결`
