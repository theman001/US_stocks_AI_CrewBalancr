# Stage 3b-2 검토 — ⑥ PM + ⑦ Risk Officer + 반려 루프 (crewai.Flow)

## 스코프

### 신규
- `aegisvest/agents/organization.py` — `run_organization`: 애널리스트 크루 →
  `_OrgFlow`(crewai.Flow: ⑥ PM → ⑦ Risk → @router 반려 1회 → ⑧ CIO) → org_orders.
- `aegisvest/agents/pm.py` — `clamp_pm_draft` (PM 제안 draft 하드 클램프), `eligible_pool`.
- `tests/test_agents/test_pm.py`, `tests/test_agents/test_organization.py`.

### 변경
- `aegisvest/agents/crew.py` — `run_crew` → `run_analysts`(①②③④⑤) + `make_task` 팩토리 노출.
  `AnalystBundle`. `_HEDGE_ONLY_TASKS` 에 pm_draft 추가.
- `aegisvest/agents/__init__.py` — `quiet_crew_console()` (crewai rich Panel 억제).
- `aegisvest/pipeline.py` — `build_orders` / `category_usd` 공개 (조직·결정론 공용),
  `run_pipeline` 은 `build_orders` 재사용.
- `aegisvest/main.py` — `run_organization` 호출, CIO APPROVED 면 `org_orders` 체결
  (`use_org` 플래그로 빈 리스트도 존중), `_bump_cooldown` 은 실제 체결 주문 기준.
- `aegisvest/report.py` — `_crew_notes` (PM 틸트·Risk 검토 추가).
- `aegisvest/schemas.py` — `PMDraft` / `RiskReview`, `CrewOutcome` 확장 (pm_draft /
  risk_review / risk_rounds / org_orders / rebalance_held).
- `config/{agents,tasks}.yaml` — portfolio_manager / risk_officer.
- `report/phase-3 §5` — ⚠️ 사용자 결정 노트 (PM 이 draft 직접 제안 + 파이썬 클램프).

**사용자 결정 2026-09-01**: (1) PM 이 DraftPortfolio 를 직접 출력, `clamp_pm_draft` 가
하드 클램프. (2) 반려 루프는 `crewai.Flow @router`. (3) 섀도 이중 추적은 3b-3.

## Round 1 — 4건

- [논리] `clamp_pm_draft` 틸트 기준이 최종 목표(`allocation.category_targets_total`) →
  램프업 중 org draft 가 결정론 초안 대비 14%p 점프 → `check_constraints` FAIL
  (max_change_per_rebal 10%p). → 기준을 `pr.draft.category_weights`(이번 회차 결정론
  배분)로 변경. PM 재량은 그 위 ±3%p.
- [논리] PM 이 카테고리 예산보다 많은 종목 제안 → 티어 하한(3%) 강제로 카테고리 합이
  예산 초과 (±3%p 위반). → 예산이 담을 종목 수 `n = int(budget/lo)` 로 컷(확신 낮은 순),
  + 하한 상향으로 초과 시 비례 축소.
- [논리] PM 이 카테고리를 아예 안 건드리면(종목 0개) 그 카테고리가 0 으로 → -10%p
  (±3%p 위반). → `_clamped` 에서 PM 미언급 카테고리는 결정론 종목 그대로 머지.
- [논리] main: `org_orders == []` 를 "조직 실패"로 오인해 결정론 주문으로 폴백 →
  조직이 "이번 주 매매 없음"을 의도해도 무시. → `use_org` 플래그로 조직 승인 시
  빈 리스트도 그대로 사용.

수정 후 ruff / format / mypy(45) / pytest(196, +1 deselected) 통과.
라이브: 램프 0.1/0.1/0.1 → org 0.1/0.13/0.1 (mid +3%p), constraints PASS.

## Round 2

- 재검토 0건. 라이브 스모크(mid overweight, PM 부분 제안) constraints PASS 확인.

### 검토했으나 finding 아님 / 후속
- crewai.Flow `kickoff()` 안에서 `Crew().kickoff()` 중첩 — crewai 1.15 에서 동작
  (테스트 green). 업그레이드 시 재확인.
- `async_execution`(①②③④) + 반려 루프 — 실 DeepSeek(잔액부족) 미검증. `pytest -m llm`.
- PM 이 소수 종목만 제안 시 카테고리 예산 미달분은 현금 (클램프가 포지션을 만들지 못함) — 의도.
- 섀도 A/B 이중 포트폴리오(deterministic vs org 병행 NAV) — **3b-3**. 현재 main 은
  단일 pf 에 org_orders 체결, `shadow.deterministic = pf`.
- ⑦ Risk 의 `CONDITIONAL` verdict 은 3b-2 에서 APPROVED 와 동일 취급 (조건 수용은 3b 후속).

## 최종
- ruff / ruff format / mypy(45 files) / pytest **196 passed, 1 deselected** 통과.
- 커밋: `build(3b-2): ⑥ PM + ⑦ Risk Officer + crewai.Flow 반려 루프`
