# Stage 3a-5 검토 (스크리너 + 스코어링)

## 스코프

**신규**
- `tools/_derived.py` — Piotroski F / Altman Z / 배당 연속증배·성장률 / ROIC / 평균 ROE (순수 함수)
- `tools/_screen.py` — 공용: `merged_values` (fundamentals+market_data 병합), `check_filter`, `apply_op`
- `tools/universe.py` + `config/universe/{sp500,nasdaq100}.txt` — 유니버스 (198종목 정적)
- `tools/screener.py` — `screen(category, universe, limit)` → 하드 필터 통과 종목
- `tools/scoring.py` — `score_category(category, tickers, theme_strength_overrides)` → percentile 스코어
- `tools/breadth.py` — `market_breadth(tickers)` → 시장 폭 (레짐 breadth 축, 3a-8 배선)
- `config/filters/{low,mid,high}.yaml` + `config/scoring/{low,mid,high}.yaml`
- 6 test files (test_derived / test_universe / test_screener / test_scoring / test_breadth + fundamentals 재작성) — 총 101 tests

**변경 (영향)**
- `tools/fundamentals.py` — **FMP → yfinance 전면 재작성** (FMP 무료 티어가 대부분 종목의 TTM 엔드포인트를 402 제한). `Ticker.info` + 3개 재무제표(5년) + `.dividends`. 파생지표 실제 계산.
- `tools/news.py` — FMP 뉴스 제한(402/404) → 종목 뉴스도 Google News RSS.
- `tools/macro_data.py` — KC Fed series id 제거 (검증 실패). Empire/Philly/Dallas 3개.
- `rules.py` — `FilterSpec` / `CategoryFilters` / `ScoringComponent` / `SubtierRule` / `CategoryScoring` + 로더.
- `schemas.py` — `Fundamentals` 에 `rev_growth_yoy` / `debt_to_equity`, `Screen*` / `Score*` 모델.
- `conftest.py` — 테스트에서 FMP/FRED/ISM 키 제거 (개발자 `.env` 비의존).

**영향받는 이전 단계**
- `tools/market_data.py` — `_screen.merged_values` 가 소비. 미변경.
- `tools/_prices.py` — `breadth.py` + `fundamentals._pe_5y_median` 가 소비. 미변경.

## Round 1

- **[논리]** `scoring._component_score` — `theme_strength` (llm_fed, 이미 0~1 스코어) 를 percentile rank 로 처리 → 오버라이드 없을 때 전원 1.0 → HIGH 총점 +25 인플레. **수정**: llm_fed metric 은 raw 값 clamp 사용, percentile 안 씀.

## Round 2

- 변경분(`_component_score`) 재검토 — 오버라이드 없으면 0.5 중립, 상대순위 정상. 테스트 통과. **수정 필요 0건. 검토 종료.**

---

## ⚠️ 사용자 승인 대기: phase-1 필터 임계값 재보정

라이브 검증(2026-09, yfinance 실데이터) 결과 phase-1 과제 A 의 LOW/MID 하드 필터 상당수가
현 시장 대비 과도하게 엄격해 통과 종목이 0에 수렴했다. **`config/filters/*.yaml` 에 완화값을
넣고 코멘트로 명시**했으나, phase-1 명세 이탈이므로 승인이 필요하다.

| 필터 | phase-1 | 3a-5 적용값 | 근거 (실측) |
|---|---|---|---|
| LOW `beta_60m` 하한 | 0.6 | **0.15** | JNJ β 0.21, KMB 0.25 — 우량 방어주가 초저베타 (베타 계산은 yfinance 와 일치 확인) |
| LOW `dgr_5y` | ≥ 0.05 | **≥ 0.03** | CL/KMB 배당성장률 3.3% |
| LOW `eps_payout` 상한 | 0.65 | **0.75** | 성숙 배당주 페이아웃 통상 60~75% (PEP 0.75) |
| LOW `fcf_payout` 상한 | 0.75 | **0.85** | PG 0.77 (과분배 아님). PEP 0.98 / KMB 1.76 은 여전히 탈락 |
| LOW/MID `eps_positive_years_10` | ≥ 10 | **≥ 4** | yfinance 무료가 4~5년만 제공 |
| MID `beta_60m` 하한 | 0.9 | **0.70** | V β 0.76, MA 등 결제 네트워크 저베타 |
| MID `rule_of_40` | 하드 필터 | **제외 → 스코어링만** | phase-1 "소프트웨어 한정" 이라 범용 하드 게이트 부적합 |

미변경(엄격 유지): 시가총액, `net_debt_ebitda`, `roe_5y_avg ≥ 12/15%`, `roic > wacc`,
`piotroski_f ≥ 6`, `altman_z ≥ 3`, PEG, 매출 CAGR, gross margin.

**결정 옵션**: (a) 위 완화 승인 (b) phase-1 원값 유지 (통과 종목 희소) (c) 개별 항목 조정.

라이브 스크리닝 결과 (SP500 앞 70종목 + 개별 검증):
- LOW 통과: ECL, GD, GILD, ADP, PG, MCD 등 (완화 후 ~10%, 200 유니버스 확장 시 ~15종목 예상)
- MID 통과: MSFT, V, MA, META (GOOGL 은 forward EPS 성장 -25% 로 탈락 — 정상)
- HIGH 통과: NVDA, EOG 등

## 이월 (후속 단계)

- **[성능]** yfinance 티커당 ~5 호출, `.info` 느림. 200 유니버스 uncached ≈ 10분 (24h 캐시 후 빠름). 3a-8/주간 크루는 `limit` 없이 전량, 백테스트는 별도.
- **[3b]** `eps_revision_3m`, `credit_rating` — yfinance 에 소스 없음 → None (관련 필터 optional 처리).
- **[3a-6]** `max_positions` 는 scoring yaml 에 기본값 (paper 20/15/15). NAV 티어 확대 로직은 allocation.
- **[3a-8]** `breadth.market_breadth` 를 유니버스로 호출해 `MacroData.pct_above_200dma` 패치 → 레짐 5축 → 6축.
- **[JNJ altman_z]** yfinance 가 최신년도 `Retained Earnings` 를 NaN 으로 주는 경우 있음 → `_derived._fill` 이 직전년도로 보완.
- **[docs/TOOLS.md]** §8+ 번호가 신규 §7.1/§7.2 와 개념적으로 중첩 (문서 전체 재번호는 미실시).

## 최종

`ruff check .` / `ruff format --check` / `mypy aegisvest` / `pytest -q` (101 passed).
라이브: fundamentals(ABT/KO/JNJ/MSFT), screen(LOW/MID/HIGH), score_category(LOW/MID), macro_data(FRED 완전) 검증.
