# Stage 4-5 검토 — 거버넌스 CLI (`python -m aegisvest.diary review`)

## 스코프

### 신규
- `aegisvest/diary/governance.py` — `report_text()` (pending_review 큐 + 신규 태그 +
  top-20 회상 교훈 + base rate), `approve`/`retire`/`edit_lesson`/`acknowledge_tags`,
  `run_cli(...)`.
- `aegisvest/diary/__main__.py` — `python -m aegisvest.diary review [--approve/--retire/
  --edit --lesson/--ack-tags]` argparse 디스패처.
- `aegisvest/diary/rag.py::_log_recall` — `recall()` 결과 entry_id 를 `state/diary/
  recall_log.jsonl` 에 append (§8 "가장 많이 회상된 교훈" + 4-7 O-D 신호).
- `tests/test_diary_governance.py` — 6 tests.

### 영향 (검토 포함)
- `DiaryEntry.rag_status`/`post_mortem`/`lesson_card`/`lesson_vector_id` — governance 가 write.
- `rag.DiaryRAG.index` — `retired`→purge, `lesson_vector_id is None`→재임베딩 로 governance
  액션이 다음 배치에 반영 (이미 4-3 에 존재하는 경로).
- `diary/logger.{load_entries,save_entries}` — 전체 rewrite (일관).
- `rag.recall()` — 반환 직전 `_log_recall` 1회. 콜드스타트 조기반환은 로그 안 함.

## Round 1 — 0건

### 검토했으나 finding 아님 / 의도된 단순화
- **§8 "신규 교훈이 기존 고신뢰 교훈과 충돌 → 플래그 목록" 미구현** — 교훈 간 임베딩
  유사도 비교가 필요해 4-6 (튜닝) 로. 지금은 `post_mortem.flags` (4-2 모호성 플래그) 표시.
  `report/phase-4 §8` 결정 노트.
- **`recall_log.jsonl` append-only 무제한** — 주 ~1행(주간 크루 1회 회상), 연 ~50행.
  로테이션 over-engineering. ponytail 주석.
- **governance 액션이 chroma 를 직접 안 건드림** — `rag_status`/`lesson_vector_id` 만 바꾸고
  재색인은 다음 `python -m aegisvest.diary.rag` 배치. 4-3 의 idempotent index 경로 재사용,
  거버넌스 CLI 가 무거운 bge-m3 를 로드하지 않음.
- **`retire` 가 미색인(pending_review) 항목에 대해 `counts["retired"] += 0`** — `_purge` 가
  삭제할 벡터 없음. 무해.
- **`--edit` 는 `--lesson` 없으면 `SystemExit`** — argparse 관례.

## Round 2 — 0건

Round 1 이 이미 0건. `_log_recall` 추가의 역방향 영향 (test_diary_rag 의 recall 테스트가
tmp 에 로그 파일 생성) 확인 — fire-and-forget, 반환값·기존 assert 무영향. 243 passed.

## 최종
- ruff / ruff format / mypy(50 files) / pytest **243 passed, 2 deselected** 통과.
- 커밋: `build(4-5): 거버넌스 CLI (python -m aegisvest.diary review) + recall_log`
