# Stage 3a-9 검토 — 에이전트 3 + 가드레일 + 판단 일기 훅

## 스코프

### 신규
- `aegisvest/llm.py` — DeepSeek LLM 팩토리 (`get_llm`), `LLMUnavailableError`
- `aegisvest/agents/guardrails.py` — `no_fabricated_numbers` (헤지어+숫자 / 페이로드 외 수치 거부)
- `aegisvest/agents/crew.py` — `run_crew` (① Macro Strategist → ②③④ 통합 Analyst → ⑧ CIO)
- `aegisvest/agents/__init__.py` — CrewAI 텔레메트리 차단 (import 시점)
- `aegisvest/diary/schema.py` — claim_type 통제어휘, horizon 일정, 태그 파생
- `aegisvest/diary/logger.py` — `log` / `load_entries` (state/diary/entries.jsonl append)
- `config/agents.yaml`, `config/tasks.yaml`
- `tests/test_agents/{conftest,test_crew,test_guardrails}.py`, `tests/test_diary.py`

### 영향 (impact scope)
- `aegisvest/schemas.py` — MacroBrief/AnalystNote/AnalystView/CIODecision/CrewOutcome/DiaryEntry
- `aegisvest/notify.py` — `post_agent_note` (기존 `post` 재사용)
- `aegisvest/config.py` — `deepseek_api_key`/`model`/`reasoner_model`/`llm_temperature` 소비 (기존 필드)
- `PipelineResult` (3a-8) — `crew.build_inputs` 가 읽기 전용 소비
- `pyproject.toml` — `uv sync --extra agents` (crewai 1.15.18), `[tool.pytest] filterwarnings` +
  `-m "not llm and not net"`

근거: report/phase-3 §3(①②⑧)·§4·§5·§6·§9, phase-4 §2·§10, docs/PROMPTS.md, add-agent 스킬.
**사용자 결정 2026-09-01**: (a) 지금 전체 구축, mock LLM 우선, (b) CIO 에 HOLD 권한 부여
(이번 주 리밸런싱 보류만 — 주문 수량·비중 불변).

## Round 1 — 5건

- [논리] `guardrails._NUM` 정규식이 맨 정수까지 검사 → "S&P 500", "200일선" 같은 참조·이름을
  페이로드 외 수치로 오탐, 애널리스트 정성 서술을 반려. → 정량 주장(%·소수)만 검사, 참조
  상수(20/50/100/200/500 등) 화이트리스트.
- [논리] guardrail `allowed` 집합이 좁아 CIO 가 "고위험 20% 상한" 같은 정당한 인용도 반려 가능.
  → `_num_payload` 를 배분(sleeve/cash/targets/**guardrails**)·포지션 비중·축 점수·스크린 수까지
  포괄하도록 확장.
- [논리] CrewAI 가 커스텀 `BaseLLM` 경로에서 `output_pydantic` 자동 변환을 안 함 (mock 테스트).
  → `_coerce`: `.pydantic` → `.json_dict` → raw JSON 파싱 폴백. 콜백·`_out` 양쪽에 적용.
- [논리] CrewAI 가 guardrail 함수의 **반환 어노테이션을 런타임 검증** — `from __future__ import
  annotations` 로 문자열이 되면 거부. → 내부 `_check` 반환 어노테이션 제거.
- [성능/프라이버시] CrewAI 익명 텔레메트리 전송. → `agents/__init__.py` + `llm.py` 에서
  `CREWAI_DISABLE_TELEMETRY` / `OTEL_SDK_DISABLED` setdefault (crewai import 전).

수정 후 ruff / format / mypy(39) / pytest(174, +1 deselected) 통과.

## Round 2 — 1건

- [논리] `llm.py` 가 `aegisvest.agents` 를 거치지 않고 직접 import 될 경우 텔레메트리 차단이
  누락. → `llm.py` 자체에도 setdefault (crewai import 전, idempotent).

수정 후 전체 통과.

## Round 3

- 수정 필요 0건. 검토 종료.

### 검토했으나 finding 아님
- 출력 스키마에 `source_tool`/`source_call_id` 필드 없음 — 3a-9 세 에이전트는 숫자 필드를
  안 낸다(해설·판단·verdict). 인라인 텍스트 숫자는 guardrail 이 커버. PM(틸트 %) 도입되는
  3b-2 에서 provenance 필드 추가.
- 애널리스트 `excluded_tickers` 는 3a-9 에선 **권고만** (일기에 `enforced: False` 기록,
  주문 불변). 실제 제외는 3b PM. phase-3 §9 목록에 PM 없음 → 의도된 범위.
- 뉴스(④ News Analyst 툴)는 3a-9 크루에 미연결 — 3b-1. Macro/Analyst description 에서 명시.
- `_coerce` raw JSON 추출은 `{`~`}` 최외곽 휴리스틱 — 실 DeepSeek 는 native 구조화출력
  경로라 미사용. mock 전용.

## 라이브 검증 (부분)
- 실 DeepSeek 키 인증 **성공** (crewai → OpenAI-호환 provider → `api.deepseek.com` 도달 확인).
- 크루 전체 실행은 **DeepSeek 계정 잔액 부족(402 Insufficient Balance)** 으로 미완.
  → `tests/test_agents/test_crew.py::test_live_deepseek_crew` (`@pytest.mark.llm`, 기본 deselect).
  잔액 충전 후 `uv run pytest -m llm` 로 검증. DeepSeek 가 `beta.chat.completions.parse`
  (json_schema 구조화출력) 를 지원하는지도 이때 확인 필요.
- mock(ScriptedLLM) 로 크루 파이프라인·콜백·일기·가드레일 전 경로 검증 (16 tests).

## 최종
- ruff / ruff format / mypy(39 files) / pytest **174 passed, 1 deselected** 통과.
- 커밋: `build(3a-9): 에이전트 3 (Macro/Analyst/CIO) + no_fabricated_numbers + 판단일기 훅`
