# Stage 4-2 검토 — ⑨ Performance Reviewer (post_mortem + 태깅 + lesson_card)

## 스코프

### 신규
- `aegisvest/diary/reviewer.py` — `run(llm=None)` (채점완료 미반성 항목 → post_mortem/
  lesson_card/태그 append 또는 `gated`), `rag_status_for` (§4.3 게이트), `_validate`
  (모호 반성 → pending_review), `_cap_lesson_card` (25토큰 절삭), `_merge_tags`
  (SEMI-OPEN 검증 + `pending_tags.json`), `python -m aegisvest.diary.reviewer` CLI.
- `aegisvest/agents/crew.py::run_reviewer` — 단일 태스크 크루 (전용 Agent, 툴 없음,
  guardrail hedge_only).
- `config/agents.yaml` `performance_reviewer` + `config/tasks.yaml` `post_mortem`
  (사후확신 편향 방지 강제 구조).
- `aegisvest/schemas.py::ReviewerOutput` (LLM 출력 모델).
- `tests/test_diary_reviewer.py` — 10 tests (mock LLM, 시나리오 8).

### 변경
- `aegisvest/schemas.py::DiaryEntry.post_mortem` `str | None` → `dict[str, object] | None`
  (§4.1 은 중첩 객체 — `outcome` 와 동일 취급). 읽는 곳 없음 (정의만 존재).
- `config/diary_taxonomy.yaml` 주석 — 존재하지 않는 `_SAFE_EVAL` 참조 → `eval_signal_rules`.
- `CLAUDE.md` / `docs/BUILD_PLAN.md` / `docs/TESTING.md` / `report/phase-4 §4·§10`.

### 영향 (검토 포함)
- `diary/evaluate.py::needs_reflection` — reviewer 가 import·호출 (읽기).
- `diary/logger.py::{load_entries,save_entries}` — reviewer 가 전체 rewrite (evaluate 와 동일 패턴).
- `agents/crew.py::{_cfg, _out, _coerce, no_fabricated_numbers, quiet_crew_console, _ABSOLUTE_RULES}`
  — `run_reviewer` 가 재사용. `_agents` 는 **미사용** (전용 Agent 인라인 생성 → 주간 크루에
  9번째 idle agent 누출 방지).
- `agents/organization.py` / `run_analysts` — `_agents`/`_cfg` 공유하나 `performance_reviewer`
  키만 추가돼 기존 인덱싱 무영향. 회귀 테스트 통과.

## Round 1 — 4건

- [논리] `reviewer._record_pending_tags` — `pending_tags.json` 이 dict 가 아닌 JSON(list 등)
  이면 `existing.get()` AttributeError 로 배치 중단. → `isinstance(loaded, dict)` 가드.
- [코드] `reviewer._cap_lesson_card` — `card[:75].split()[:25]` 이중 상한이 맞으나 의도
  불명확 + 공백 미정규화. → docstring + `" ".join(card.split())` 선행.
- [코드] `run()` `counts["flagged"] += _apply(...)` — `bool` 을 int 에 가산 (동작은 OK).
  → `int(_apply(...))` 명시.
- [논리/idempotency] `needs_reflection` 아닌 `evaluated` 항목이 status 그대로 남아 매주
  재게이트 (카운트 누적·무의미). → `status = "gated"` 종결 상태 추가 (reflected 와 함께
  RAG 진입, 재실행 시 스킵). 테스트 + docstring 반영.

수정 후 ruff / format / mypy(47) / pytest **222 passed, 2 deselected** 통과.

## Round 2 — 0건

### 검토했으나 finding 아님 / 의도된 단순화
- `_VAGUE` 정규식이 "신중"·"주의" 를 포함한 정당한 휴리스틱도 플래그할 수 있음 —
  **의도적 보수 설정** (오탐 → 인간이 pending_review 에서 승인, 미탐 → 나쁜 반성이 RAG 자동
  진입). 4-6 튜닝 대상.
- `lesson_card` 토큰 카운트는 공백어수·문자/3 근사 — 한글 실 토크나이저 아님. 명시적
  4-6 튜닝 항목, `report/phase-4 §4` 에 기록.
- Reviewer 에 NewsScraper 툴 미제공 — §3 표는 소비 툴로 명시하나, point-in-time 뉴스가
  없는 현 인프라에서 라이브 RSS = 사후정보라 §4.2(하인드사이트 방지) 와 배치. 기록시점
  필드로 대체, Sharadar 시 정밀화 ([[backtest-deferred]] 와 동일 근거). §4 결정 노트.
- `mistake:none` 태그 저장 생략 — 검색 구조매칭(§7.3)에 mistake 축이 없어 순수 노이즈.
- 채점완료·미샘플 `hit` 은 `gated` 로 종결되고 lesson vector 없이 situation vector 만
  RAG 진입 — §5.1 이중벡터 설계대로.
- `run()` 이 변경 0건에도 `save_entries` 전체 rewrite — evaluate.py 와 동일, 일기 규모
  (연 수백건) 무시.
- 실 DeepSeek 미검증 (계정 잔액) — `pytest -m llm` (`test_live_deepseek_reviewer`).

## Round 3 — 0건

`gated` 상태 추가의 역방향 영향 재확인: 일기 status 를 읽는 곳은 evaluate.py(`!= "open"`)
와 reviewer.py 뿐. 4-3/4-4 미구현. 회귀 없음.

## 최종
- ruff / ruff format / mypy(47 files) / pytest **222 passed, 2 deselected** 통과.
- 커밋: `build(4-2): ⑨ Performance Reviewer — post_mortem·태깅·lesson_card (하인드사이트 하네스)`
