# Stage 4-1 검토 — diary/evaluate.py (결정론 채점)

## 스코프

### 신규
- `aegisvest/diary/evaluate.py` — `run(today=None)` (evaluate_after 도래한 open 항목 채점 →
  status evaluated/expired + outcome), `needs_reflection(entry)` (Reviewer 큐 게이트 §3.3),
  `python -m aegisvest.diary.evaluate` CLI. 3 채점 계열 (섀도 델타 / 초과수익 / regime_call)
  + event_risk 근사.
- `tests/test_diary_evaluate.py` — 합성 데이터 11 tests (시나리오 8).

### 변경
- `aegisvest/diary/logger.py` — `save_entries(entries)` (전체 rewrite — jsonl 단순화판),
  `__all__` 갱신.

### 영향
- `DiaryEntry`(outcome/status 필드 — 3a-9 부터 존재), `_prices.history`, `ShadowState`/
  `RegimeHistoryPoint`/`Label`/`regime_rules`, `state.load_model/load_list` — 전부 읽기.
- `report/phase-4 §3` — 3계열 설계 + SPY 벤치마크 근사 + event_risk 방향미검증 결정 노트.

## Round 1 — 1건

- [논리] `run()`: `evaluate_after` 가 빈 리스트인 손상 항목이 영구 `not_due` 로 남아
  채점도 만료도 안 됨. → 빈 리스트는 `status=expired`.

수정 후 ruff / format / mypy(46) / pytest(213, +1 deselected) 통과.

## Round 2 — 0건

### 검토했으나 finding 아님 / 의도된 단순화
- `_nav_at_or_before` 가 호출마다 history 정렬 (엔트리당 4회) — 일기 규모(연 수백건 ×
  작은 히스토리)라 무시. yfinance/mark_to_market 이 이미 시간순이라 정렬은 방어적.
- `save_entries` 전체 rewrite 시 `load_entries` 가 스킵한 손상 라인은 영구 삭제 — 이미
  기능적으로 못 쓰는 라인이라 수용. 포렌식 보존은 over-engineering.
- SPY 벤치마크 근사 (vs "카테고리 중앙값"), event_risk 방향 미검증 (attribution 항상 low),
  regime_call 4주 예비 생략 — 스키마·point-in-time 인프라 한계. report/phase-4 §3 에 명시.
  3a-11 백테스트 보류와 같은 근거 ([[backtest-deferred]]), Sharadar 도입 시 정밀화.
- 임계 상수는 실데이터 축적 후 4-6 튜닝. 지금은 배관·경계 로직 검증이 목적.
- `needs_reflection` 의 hit 샘플링은 `random.Random(entry.id)` — PYTHONHASHSEED 무관,
  항목별 재현적 (idempotent 재실행). 테스트로 확인.

## 최종
- ruff / ruff format / mypy(46 files) / pytest **213 passed, 1 deselected** 통과.
- 커밋: `build(4-1): diary/evaluate.py — 결정론 채점 (섀도델타·초과수익·regime_call)`
